# ... (passos anteriores) ...

      - name: ⚙️ Executar 'calcular_risco_cli.py' (Calcula e incrementa o histórico)
        run: python calcular_risco_cli.py
        
      # --- ETAPA 3: CORREÇÃO E ENVIO DO HISTÓRICO AGREGADO PARA O PAINEL ---
      # Usaremos o step-by-step de GIT para garantir o commit do arquivo ÚNICO.
      - name: 🚀 Enviar 'resultado_risco_final.csv' para o Repositório do Painel
        run: |
          # 1. Configura as credenciais (usando o token de destino)
          git config --global user.name 'GitHub Actions Bot'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          # 2. Clona o repositório de destino para uma subpasta temporária (usando o PAT)
          # NOTA: O token REPO_DESTINO_TOKEN DEVE ser passado diretamente na URL para autenticação
          DEST_REPO="RafaellaB/Painel-Diagrama-de-Risco"
          DEST_BRANCH="main"
          
          # URL com o token inserido
          REPO_URL="https://${{ secrets.REPO_DESTINO_TOKEN }}@github.com/${DEST_REPO}.git"
          
          # Clona e entra na pasta
          git clone --single-branch --branch ${DEST_BRANCH} ${REPO_URL} destino_repo
          cd destino_repo
          
          # 3. Copia o arquivo gerado (risco_final.csv) para o repositório clonado
          # O arquivo foi gerado na raiz do repositório original.
          cp ../resultado_risco_final.csv .
          
          # 4. Faz o commit e push
          git add resultado_risco_final.csv
          
          # A opção '|| exit 0' permite que o job continue se não houver alterações (sem commit)
          git commit -m "Incremento diário de risco: Adicionado dado do dia que encerrou." || exit 0
          git push origin ${DEST_BRANCH}